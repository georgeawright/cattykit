# Cattycam

Cattycam is an interactive tool for logging and debugging artificial intelligence models built using Cattykit.

Browse a Cattycam SQLite history database with:

```shell
panel serve cattycam/app.py --show --args path/to/history.sqlite
```

The app displays the logs from each recorded run.
